# 核心候选独立审查回执

日期：2026-09-22。审查者：独立只读代理 `botsort_core_review`。范围：核心与原跟踪器集成、输入和生命周期契约、候选消费、真实序列与浏览器验证、双语报告和门户规划回执。

## 发现与处理

- 核心审查没有严重或重要阻塞项。设计中大间隔与矩阵检查的顺序描述已同步实际契约；不改变错误时状态不推进的要求。
- 交付审查发现消融脚本原先仅引用主评测摘要中的候选哈希，未读取当前构建物进行比较。已在创建输出目录之前计算并断言当前候选哈希一致，摘要记录实际值；在新的 `ablation-verified` 目录重跑消融和评分，结果保持一致。
- 浏览器脚本补充完整运行状态和适配器哈希核验，在新回执中重新验证完整 05 序列。保留先前输出，没有覆盖历史证据。

## 最终核对

复查当前源码重建的 ESM/CJS、31 份来源文件、21 份 MOT、两次非耗时摘要、固定输入、浏览器和评分绑定，未发现未解决项。报告复查确认 09 消融差值与原始评分一致，准确区分历史探针、当前共享核心以及公开版本，并保留噪声归因、性能范围和浏览器能力的限制。

最后一轮文档审查为只读对照，没有重新测量。实际运行证据见 [验证回执](validation.json)；原始日志、来源锁和 [归档校验脚本](verify.mjs) 随报告保留。此审查不是第三方认证或全平台兼容承诺。
