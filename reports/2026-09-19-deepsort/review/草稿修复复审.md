### Finding Verdicts

- **样例切换会把未应用的参数草稿提交为实际配置** — **ADDRESSED**。`demo/src/App.tsx:129` 已改为把 `session.options` 传给 `prepareSequence`，因此样例替换沿用当前已生效配置；参数草稿仍只会在表单提交路径中转换并生效。`tests/browser.mjs:55` 在把 OC-SORT 草稿 `ocmWeight` 改为 `0.9`、不点击应用并切换样例后，断言导出的实际值仍为 `0.2`，直接覆盖原缺陷路径。

### New Breakage in the Fix Diff

- 无 Critical/Important 新破坏。DeepSORT 当前配置可能携带自定义 2D `featureSpace`，而内置样例声明合成 4D 空间；`prepareSequence` 会以输入顶层声明覆盖当前空间。新增 `tests/browser.mjs:91` 的回归路径验证从自定义空间切换内置样例后，导出的顶层空间、实际 options 和帧标识均恢复为合成 4D 空间，且后续非法导入仍保留该会话。

### Out-of-Scope Observations

- 无。

### Verdict

**Fix round：** All findings addressed, no new Critical/Important breakage。

检查记录：已读取修复 diff 与任务报告追加证据。RED 明确得到 `0.9 !== 0.2`；GREEN 记录 Demo typecheck/build 退出码 0、11 组浏览器检查通过且 `pageErrors=[]`。按范围复审要求未重跑测试，也未审查修复 diff 之外的代码。
