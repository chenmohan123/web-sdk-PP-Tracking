# Tracking ReID 阶段最终只读审查

日期：2026-09-21。审查范围：门户 `8b80ea8..e8aa45b`、SDK `4bbd3d5..51c4e2e`。依据实施计划、progress、Task 1/2 独立审查及修复回执、Task 3 集成回执，读取给定最终差异并重点核对跨层契约、候选实现、浏览器脚本、双语文档和机器证据。未修改源码、索引或 HEAD，未执行远程操作，未重跑完整测试矩阵。

本文路径前缀 P 指门户工作树，S 指 Tracking SDK 工作树，均与实施计划一致。

## 规格符合性

**Task 3：通过。阶段整体：通过。** 本轮交付是标准 1.3.0 与同包 ReID 源码候选；正式 exports、hybrid 生产 manifest、模型分发和 Demo 暂不接入属于明确裁定，不是实现遗漏。

| 核对项 | 结论与证据 |
| --- | --- |
| 独立参考与真实模型 | S `tests/reid-browser.mjs:15` 从上轮 Paddle 原始向量读取期望，并核对模型及 RGBA 字节身份；双后端各 34 组，唯一 clipped 框先拒绝再显式转换，未放宽生产输入契约。 |
| 后端和关联边界 | S `src/reid/ort.ts:18` 单独指定执行提供者并禁止 GPU 的 CPU EP 回退；S `tests/reid-browser.mjs:93` 读取实际 GPUDevice 信息，`:131` 确认关联仍是 CPU。重复帧仅用于接口验证。 |
| 取消和缓存 | S `tests/reid-browser.mjs:125` 验证取消后再次提取；运行期间等待与释放另有生命周期单测。真实 CacheStorage、坏缓存、短响应、不换源及清理隔离均有断言；`:196` 起使用真实本机 TLS gzip，区别于失败的路由压缩模拟。 |
| 默认包边界 | S `tests/reid-browser.mjs:65` 覆盖根算法运行及候选空闲工厂无 ORT/ONNX 请求；`evidence/package-check.json` 保存三算法 ESM/CJS/类型消费及文件白名单；package 差异仅新增候选脚本和开发依赖，没有公开子入口或生产依赖。 |
| 耗时和日期证据 | load/extract 分别用外围时钟记录总耗时；提取包括输入复制、Tensor 创建、回读及释放。双语报告明确工厂模型复制不计入 load，未据此声称视频 FPS 或跨设备兼容。 |
| 验证范围准确 | 完整 verify 的 138 单测与 12 浏览器检查属于 `6fc6e5a`；修复后记录 37 生命周期测试、类型/候选构建和最终真实浏览器验收，没有伪报再次完整执行。门户 130 初版、修复 35+40、21 页构建与 0/0/7 检查范围也明确。 |
| 发布边界 | P `reports/tracking/2026-09-21-reid-module/README.md:31` 将标准 before/after 的 17 项 required 通过限制为 algorithm 发行核心；双语候选指南保留许可依据、真实双源、正式入口/Demo、真实 MOT 质量的后续门槛。 |

## 做得好的部分

- 标准的 Schema、模块并集、实际后端声明、静态导出检查与产品分阶段交付一致；Task 1 的两项 Important 已修复，Task 2 的压缩长度和缓存取消问题也完成聚焦复审。
- S `src/reid/extractor.ts:25` 在执行异步工作前登记活动操作，输入快照、整帧提交及资源释放的边界清晰；本地字节、远程下载与缓存路径共用固定模型完整性约束。
- S `reports/2026-09-21-reid-module/verify_archive.mjs:8` 验证归档字节身份，`:17` 验证 12 个候选源码文件的 LF 身份，`:33` 从独立参考重算向量。报告保存失败诊断，也准确限定本机受控资源和真实 hub 的证据差别。
- 中文、英文候选指南与阶段报告对模型身份、预处理、生命周期、包范围及未完成的发布门槛保持一致。没有将源模型许可或实际跟踪质量扩大为新增承诺。

## 问题

### Critical（必须修复）

无。

### Important（应修复）

无。

### Minor（可改进）

无。

## 本审查执行的验证

仅运行 `node reports/2026-09-21-reid-module/verify_archive.mjs`，退出码 0：27 份锁定文件通过，12 份源码身份通过，双后端各 34 条归一化向量复算及缓存记录通过。另读取已有完整 verify、最终浏览器、gzip 诊断和门户检查记录；未新增模型推理或复跑完整测试。

该离线验证证明现有记录内部一致及其与当前候选源码的对应关系，不能替代新平台实测。浏览器脚本中的立即取消覆盖入口取消和恢复；单次 run/回读期间等待释放的详细语义由底层 ORT 依赖替身单测覆盖，本审查未把二者混称为真实浏览器运行中抢占。

## 建议与质量结论

按既定下一阶段补齐真实分发及许可依据后，再统一启用公开子入口、hybrid manifest 和 Demo；本轮不需要追加生产能力或远程动作。

**可本地交付／进入后续阶段：是。** 三项任务满足本轮范围，未发现新增 Critical、Important 或 Minor；此前问题均已解决，标准、候选源码与验收材料之间没有阻塞性矛盾。该结论不构成正式 hybrid 发布合规、远程合并或模型分发授权。
