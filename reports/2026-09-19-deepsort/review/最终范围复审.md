# DeepSORT 最终 F1 范围复审

日期：2026-09-19。固定范围：`b349bcf..4833dbf`。已完整读取修复差异包、`final-fix-report.md` 和差异所附 RED/GREEN、浏览器及包检查原始证据。本次仅复核 F1 与修复引入的新严重问题；未重新审查整阶段、未派代理、未重跑全套测试，未读取上游实现。`preview-final/` 不纳入本次范围。

## 原问题裁定

**F1（合法输入导出的 JSON 超过自身导入上限）：ADDRESSED。**

- `demo/src/data.ts:52` 的 `serializeSequence` 只输出 `frames` 和可选 `featureSpace`，采用紧凑 JSON，将 `Float32Array` 转为可重导入的普通数组，并以 `TextEncoder` 的 UTF-8 字节数限制5 MiB。
- `demo/src/data.ts:120` 在临时 tracker 创建与逐帧模拟之前使用同一函数检查规范化输入大小。超限输入没有机会替换当前会话；成功准备的数据与输入导出使用相同序列化规则，消除了原本缩进、运行结果和元数据造成的大小放大。
- `demo/src/App.tsx:155` 增加独立输入下载，`demo/src/App.tsx:182` 在尚未单步时允许下载、读取或播放期间禁用；原结果报告仍保存实际配置与结果。`README.md:39`、英文镜像及相关指南明确只有紧凑输入保证重新导入，结果报告可能超过5 MiB。
- `tests/demo.test.ts:48` 覆盖原512维千帧场景、typed array转普通数组、首尾时间和向量往返；`tests/demo.test.ts:67` 覆盖动态临界值、多字节ID不能以字符串长度代替UTF-8字节数、超限准备失败及原结果/开始时间/索引保留。
- `tests/browser.mjs:101` 起使用真实下载字节，在单步前后重新上传512维1000帧输入，检查空间、帧数、时间、向量及重新导入后索引复位。归档 Chromium153 日志记录12组通过、`pageErrors=[]`。

以上文件行号均相对 SDK 根目录 `C:/Users/chenm/.codex/worktrees/tracking-algorithms/web-sdk-PP-Tracking`。

## 修复引入的新问题

未发现新的 Critical/Important 问题。新增序列化不修改会话，准备校验仍在既有原子提交之前；核心算法、版本和发布配置未改。文档变更导致 tarball 摘要变化已有独立 `package-final.json`，原包证据未覆盖改写。

## 证据与结论

已核对新增2项 RED、聚焦14/14 GREEN、Demo类型检查与构建、12组浏览器交互、三算法实际tarball消费的原始日志；本次执行只读 `git diff --check b349bcf..4833dbf` 退出0。没有将这些聚焦验证说成重新运行完整 verify。

**可本地交付：是。** 整阶段唯一未关闭的重要问题已修复并有直接回归证据；结合前次整体审查结论，`4833dbf` 可作为本地 `0.2.0-alpha.0` 候选交付。该结论不构成远程发布、真实ReID精度或移动设备兼容性声明。
