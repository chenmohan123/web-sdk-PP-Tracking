# 独立只读审查回执

日期：2026-09-21。审查对象：基线 `b8ef9c292ba4cae41d5c4a9c83a70809486f509e` 上的本地一次性探针及中英文报告。结论：可归档研究结果；Critical 0、Important 0、Minor 1。

独立审查实际读取源码与证据，完成：

- 核验七段检测、冻结特征、运动 JSONL 和全部 21 份 MOT 输出 SHA256；5316 帧、67639 检测，固定检测无裁边/空框。
- 七段三配置只读内存回放，MOT 和所有非耗时输出哈希复现运行摘要；恒等补偿逐帧复现基线。此为独立复核，没有重新测量或归档完整性能运行。
- 将本轮忽略目录源码在内存重新打包，字节与研究 `probe.js` 一致；核对矩阵坐标恢复、轴对齐包络、`J P Jᵀ`、门控最小距离与高分 EMA。
- 数学/集成测试及额外门控反例：近邻高分外观改变分配；IoU 门控外、低分阶段和 cosine 门控失败均保持纯 IoU 行为。
- 在独立评分进程校验七段 GT 哈希，复用固定 Python 和官方 TrackEval，完整结果对象与 `metrics.json` 相等。
- 核对浏览器三配置两种结果哈希与 Node 一致；中英文报告准确披露论文差异、首帧校验不足、09 退步及浏览器边界。

Minor：原研究 `score.py` 在评分前没有再次对 MOT 文件核对摘要中的 `motSha256`。本次独立审查已校验输出并重评分；新增归档 `verify.mjs --local` 再次覆盖轨迹输入和哈希检查。保留原执行脚本，避免把后补检查冒充原始执行。未来正式评测入口应先核验输出再调用评分器。

固定检测有 6 个分数恰为 0.5，三配置均沿用相同包含等号语义，公平比较不受影响；论文边界差异已记录。所有实际矩阵有限非奇异，所以空状态校验不足不影响本次数据，但阻止直接提升为生产契约。

审查没有重新生成图片运动矩阵或 ReID 向量，没有重复测量耗时，也未修改文件、分支或远程状态。
