# 三算法真实画面评测最终整体审查

日期：2026-09-21。依据 `requesting-code-review/code-reviewer.md`，本次为只读最终审查，仅写此报告，没有修改实现、实测证据、索引、HEAD 或分支，也没有派生代理。

SDK 审查范围为 `e42cdd6..f02c9ca`，工作树为 `C:/Users/chenm/.codex/worktrees/tracking-algorithms/web-sdk-PP-Tracking`；门户范围为 `784970d..53d9a1b`，工作树为 `C:/Users/chenm/.codex/worktrees/segmentation-portal/chenmohan123.github.io`。下文文件行号默认相对于 SDK；门户文件明确标注。

## 优点与计划符合性

- **任务完整，分层边界保持。** 计划的独立工具、七段正式执行、双语归档、发布范围判断和门户路线同步均已完成。两个固定范围的实际文件清单只有评测脚本、测试、报告、说明和路线，没有 `src`、模型、默认参数、package 版本、manifest 或门户 registry/runtime 修改。两工作树 `git status --short` 为空；原用户门户工作树仍只有交接记录中的两个未跟踪路径。没有远程发布步骤被混入这次本地交付。
- **媒体准备独立且来源可追溯。** `reports/2026-09-21-mot-reid/probe/prepare_media.py:14` 固定官方 URL、大小、ETag 与七段长度；`:26` 检查远端身份，`:32` 按固定序列/帧号构造白名单，`:59` 使用 Range/If-Match，`:61` 检查响应区间和长度，`:73` 核验 ZIP 本地头/文件名/压缩方式，`:44` 核验展开长度与 CRC 并记录 SHA256。读取复用的 `reports/2026-09-19-pplcnet-reid/probe/data.py:17` 后确认仅导入 `RemoteZip`，不会执行旧研究的 GT 下载 main。协议明确 ETag/CRC 不是签名或完整 ZIP SHA，未夸大来源保证。
- **媒体到正式输入的绑定完整。** `scripts/evaluation/mot17-reid/run.mjs:35` 将输入媒体锁绑定到已归档锁，`:43` 校验 seqinfo，`:50` 校验公开 det，`:84` 每次图片服务前再检查字节、SHA 和 CRC。媒体准备不会读 GT；特征提取/跟踪只接收适配后的检测输入；GT 保持在独立评分阶段。定点计算当前准备脚本与媒体锁 SHA，分别为 `c3acd7cf659eb14b832e361c998b8dffe9d1418e51c98548ed8df64f28f1c1b0`、`906491d5d02912f38cea5014b2b687fd8b5ae4a3ec55499439ad7c049b328cb5`，与 `media-validation.json` 一致。
- **各层成本独立，结论不越界。** `scripts/evaluation/mot17-reid/browser.mjs:30` 开始本地图像流水线计时，`:50` 完整提取后才更新 DeepSORT，`:51` 在基线执行前结束其外围计时；`:55` 起单独计量两基线。模型获取/加载另有 `:11` 和 `:22` 的外围计时。报告 `reports/2026-09-21-mot-reid/README.md:61`、`:75`、`:99` 分别说明固定串行顺序、首帧/后续帧含义和模型加载另计，排除检测器、视频、渲染、评分、IPC 与落盘，不把冻结向量 Node 关联或本地图像流水线称为视频端到端。
- **实际成绩支撑有限的发布建议。** 报告 `reports/2026-09-21-mot-reid/README.md:9` 的三算法合计与独立任务 2 复核一致；`:15` 同时解释 IDF1、IDSW、MOTA、FP/FN，保留完整配置差异与缺少消融的限制。DeepSORT IDF1 45.4637% 低于 ByteTrack 48.2922%，额外图像/ReID 成本也真实列出，因此维持 ByteTrack 默认、其他算法显式可选、人体 ReID 实验状态的建议合理。门户只推进本地候选收口，版本、变更日志、发布预检和发布授权仍为后续，不声称已经发布或跨设备适配。
- **验证责任已闭环。** 任务 1 的 9 项专项与 173 项测试、构建/包检查及正式七段运行已有证据；任务 2 独立核对了 126 个正式输出、49 个计时源、压缩逐帧计时、48 处补充输出绑定和 14 份历史 MOT。本次承接这些已经完成的审查，不重复重型验证。主线程提交后 `verify --current` 通过 32 份归档；标准 before/after 为 21 required 通过、0 失败、4 remote skip；门户 build 为 0 错误、7 个已记录历史提示，不将其表述为无提示或远程合规。

## 问题

### 严重（Critical）

无。

### 重要（Important）

无阻塞本次固定结果交付的问题。

### 次要（Minor）

1. **P3：媒体准备重跑会改变本地锁，随后被正式工具拒绝。** `reports/2026-09-21-mot-reid/probe/prepare_media.py:122` 至 `:125` 将本次传输字节数写入并覆盖 `media.lock.json`。完整缓存命中时 `:113` 的 `transferred` 保持 0，而归档值为 876659951；即使所有图片完全相同，锁字节也会变化，`scripts/evaluation/mot17-reid/run.mjs:35` 的整文件 pin 随即失败。这影响重复执行准备步骤后的便利性和协议“新输出、不覆盖”约束的一致性，不会使错误图片通过，也不影响本次已核对的锁与正式结果。后续应把传输统计移出固定内容清单，或已有锁存在时核验并保留；本轮复跑使用已归档固定锁，并按该锁验证图片，不通过重写归档锁绕过门禁。无需为当前已完成的一次性研究重新生成实测身份。
2. **P3（任务 1 保留）：补充比较工具未再次绑定浏览器输出到原摘要。** `scripts/evaluation/mot17-reid/compare.mjs:39` 至 `:42` 直接比较两侧结果，未核验这些文件对原摘要的输出 hash。未来工具应复用 merge 的输出 pin 校验。本轮有 48 处独立原始输出核验，且任务 2 已再次确认，因此接受为非阻塞项，不要求修改固定工具或重跑本次正式结果。
3. **P3（任务 1 保留）：Python 子进程默认控制台编码不固定。** `scripts/evaluation/mot17-reid/run.mjs:176` 和 `scripts/evaluation/mot17-reid/merge.mjs:44` 没有在调用处固定 Python UTF-8；Windows 默认环境下拒绝诊断可能乱码。正式执行已设置 `PYTHONUTF8=1` 和 `PYTHONIOENCODING=utf-8`，协议及报告记录该条件，当前评分与 JSON 证据不受影响。后续工具迭代可显式传入编码环境，当前无需改变锁定脚本。

## 审查范围与建议

本次读取了标准入口、SDK/性能/文档发布/门户契约、完整实施计划、执行台账和任务 1/2 审查；检查固定 diff 的文件范围与报告/协议，并定点读取此前没有专项覆盖的媒体准备代码、其 Range 依赖及正式输入和计时实现。只为具体来源身份、输出范围和媒体复跑疑点增加定点检查，没有重扫整仓库、重读上游跟踪实现、重跑模型或评分，也没有把继承的测试证据写成自己重新执行。

建议将上述三项 P3 保留在收尾记录中，媒体准备问题在下一次准备数据前处理。固定实测证据保持不变；后续发布仍按新的明确范围完成版本、变更日志和发布预检。现有报告准确限定训练集、单机、补充 WASM 子集及未覆盖场景，不需要为获得更好数字修改参数或扩大本轮工作。

## 最终判断

**Ready：Yes（可以交付本次本地评测与归档；不代表授权远程发布）。**

任务从锁定真实媒体、隔离输入与 GT、完整提取/跟踪、官方评分到成本归档和推荐范围形成可核对链路，没有发现影响本轮正确性或发布边界的严重/重要问题。三项次要问题中两项已由本次补偿证据处理，一项仅影响媒体准备重跑后锁的复用；均不否定当前固定成绩和交付结论。
