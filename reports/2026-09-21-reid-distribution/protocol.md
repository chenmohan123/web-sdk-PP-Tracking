# 真实双源与生产Demo验收协议

日期：2026-09-21。本阶段只改变模型分发、公开入口和本地Demo，不修改已固定的模型数值、RGBA预处理或跟踪数学。

1. 两个平台上传固定33,704,835字节FP32模型、模型卡、许可和署名；回读不可变commit后匿名完整下载，验证SHA-256。采用官方仓库整体Apache-2.0作为官方链接checkpoint转换分发依据，独立授权与训练披露缺口如实记录。
2. 正式dist/reid/index.js在真实浏览器中使用MS/HF×WASM/WebGPU四个独立页面，各清空缓存并实际下载一次；MS省略source验证默认值。仅导入root/子入口不得请求ORT或模型，GPU必须非fallback，显式来源失败不得换源。
3. 使用此前固定Paddle参考的首个真实裁剪，独立归一化后比较512维向量。maxAbs<1e-3、余弦距离<1e-5、单位范数误差<1e-6。完整34fixture数值矩阵沿用未改变实现的上一阶段证据，此处不重复冒称新矩阵。
4. Vite生产构建使用人工PNG和人工框测试本地图像工作台，MS/WASM与HF/WebGPU各加载真实权重；验证连续帧、同尺寸换图ID、语言不复位、非法输入不推进、取消挂起下载、复位、清理及其他缓存保留。测试取消时只暂停真实请求，不用替代模型。390px中英文仅为桌面视口证据。
5. 完整verify覆盖类型、单元、两入口实际tarball消费、根调用者无需ORT、核心与Demo/Vanilla/React构建及原有浏览器流程。标准after按hybrid校验，静态通过仅表示本地合规。

人工PNG及重复图像只证明接口和生命周期，不证明真实身份、跨摄像头、时间序列质量改善或视频FPS。模型特征GPU与关联CPU分别报告。手机、其他浏览器、Worker/NPU、视频/摄像头仍未验收。

archive.mjs只收集已经完成的原始记录；evidence.lock.json固定字节，verify_archive.mjs不重建锁、不联网、不运行模型，独立复算四条向量。默认支持历史证据离线复核；--current另核对当前源码LF身份、dist及.tmp真实tarball，不适用于后续改变过的工作树。截图不含用户原图；原始图像、权重、凭据、签名重定向URL不提交。
