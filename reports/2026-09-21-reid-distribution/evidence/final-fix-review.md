# 最终修复范围复审

日期：2026-09-21。结论：**通过，Ready=Yes。F1/F2/F4 为 ADDRESSED，F3 为 ACCEPTED；修复差异未发现新增 Critical、Important 或 Minor。** 原最终报告是修复前快照，本报告记录唯一修复波次的最终处置。

## 范围

仅复审 SDK `3517d2c..8126365` 的三个文件，依据 `final-fix.diff` 与 `final-fix-report.md`；另外核对主线程当前未提交的归档脚本小范围变更。没有第二次全仓扫描、子代理、产品修改、完整测试重跑或模型下载。只写本报告。

## 逐项结论

| 发现 | 处置 | 位置与复审理由 |
| --- | --- | --- |
| F1 首载／本轮加载标签 | ADDRESSED | `demo/src/ReIdWorkspace.tsx:122` 双语均明确“本轮加载检查 / 会话复用”。保留本轮幂等 load 的真实耗时，不再让一般“加载”标题暗示所显示的是首载历史。未改变推理、关联或计时逻辑。 |
| F2 陈旧缓存估算写回 | ADDRESSED | `demo/src/ReIdWorkspace.tsx:39`、`:58` 新增独立估算序号；`:60`、`:61`、`:62` 对成功、失败、finally 都检查 mounted 与最新请求身份。`:87` 在清理开始使旧请求失效，`:56` 在卸载失效，`:107` 阻止过渡中的手动估算。旧请求不能恢复旧用量或清除新请求的忙碌状态，不改变缓存 API 与模型生命周期。 |
| F3 ORT chunk 体积提示 | ACCEPTED | 保留原最终审查裁定：847.14 kB、gzip 204.48 kB 的惰性 ORT chunk 是已测且已披露的可选模型成本。未提高提示阈值、未改变默认路径加载边界，不要求本阶段继续拆分。 |
| F4 中文根入口限定 | ADDRESSED | `docs/zh-CN/algorithm.md:7` 明确“根入口不加载特征提取模型”，与英文限定相同，也与该页可选 ReID 说明一致。 |

## 回归覆盖与证据

新增 `tests/reid-cache-ui-browser.mjs` 消费 Vite 生产页面和真实 CacheStorage 响应，在浏览器 API 边界用可控挂起制造顺序差异，未替换 UI/控制器或 SDK 估算逻辑。已读脚本全部新增内容：

- `:47` 至忙碌断言验证旧请求先完成时新请求仍禁止刷新；序号与过渡禁用共同保护此行为。
- `:58` 起持有 1 MiB 旧响应，切源后的新响应为 2 MiB，再释放旧响应，验证新用量不被覆盖。
- `:70` 起持有旧响应，完成清理并看到 0 MiB 后再释放，验证不会恢复旧用量。
- `:81` 起卸载并重新挂载，释放旧响应后新工作台仍显示未知用量且可操作。

已读取 `.tmp/reid-distribution/final-fix-cache-ui.json` 与对应日志：2026-09-21T03:33:04.128Z，Chromium 153.0.8010.12，四项检查通过、errors=[]。实施报告记录同一脚本先红后绿，及 11 项聚焦单测、Demo 类型检查、构建和前后标准检查通过；本复审未重复执行这些命令，也未据此扩大设备兼容性声明。

## 归档脚本变更

已核对当前未提交 diff：`archive.mjs` 只增加收集 `final-fix-cache-ui.json` / `.log` 和新测试脚本源码身份；`verify_archive.mjs` 只增加读取该固定回执并断言 errors=[]、checks.length=4。变更与本修复证据相符，原向量、来源、包与源码完整性断言没有删除或放宽。

主线程正在更新修复后的真实两后端生产交互、截图和最终身份锁，本复审不把尚在更新的记录称为已完成。产品修复范围已通过；最终交付仍由主线程核对刷新后的归档身份，SDK/npm/线上 Demo 发布授权边界维持不变。
