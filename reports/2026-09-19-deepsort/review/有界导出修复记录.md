# DeepSORT 最终 F1 修复报告

日期：2026-09-19  
SDK：`C:/Users/chenm/.codex/worktrees/tracking-algorithms/web-sdk-PP-Tracking`  
基线：`b349bcf`  
提交：`4833dbf`（`修复输入序列有界导出往返`）

## 改动

- 新增紧凑输入序列导出，内容仅为规范化后的 `frames` 与可选顶层 `featureSpace`；`Float32Array` 转为普通数组。
- 序列化与 `prepareSequence` 共用 UTF-8 `TextEncoder` 5MiB 上限；超限抛 `FILE_TOO_LARGE`，并在昂贵模拟前拒绝。
- Demo 新增中英文“导出输入序列”按钮；未播放前可用，读取和播放期间禁用。结果报告原按钮与内容保留。
- 双语 README、快速开始、API、隐私、排错、Demo 清单与归档说明区分输入序列和结果报告：前者保证可重导入，后者可能超过5MiB。
- 未修改核心 `src`、版本和发布配置；协调者的 `reports/2026-09-19-deepsort/preview-final/` 保持未跟踪且未提交。

## RED

命令：

```powershell
pnpm --config.verify-deps-before-run=false --config.manage-package-manager-versions=false test -- tests/demo.test.ts
```

结果：退出1；14项中新增2项按预期失败，其余12项通过。失败原因均为 `serializeSequence` 尚不存在。原始日志：`reports/2026-09-19-deepsort/final-fix-red.txt`。

## GREEN 与验证

- 同一聚焦单测：14/14通过，覆盖512维1000帧紧凑往返、`Float32Array`、首尾时间与向量、动态临界值、多字节ID的UTF-8计数、超限准备拒绝和原会话保留。
- `pnpm ... run typecheck:demo`：退出0。
- `pnpm ... run build:demo`：退出0，33个模块构建完成。
- `pnpm ... run test:browser`：退出0；Chromium 153 共12组，`pageErrors=[]`。真实下载512维1000帧输入，并在单步前后重新导入；原有配置生效与原子性回归保留。原始日志：`final-fix-browser.txt`。
- `pnpm ... run check:package`：退出0；三算法 ESM/CJS/TypeScript、零生产依赖与发行文件白名单通过。新包22,522字节，SHA256 `2a8c4aa4be4ee02698835aae18d45e27a83497676ac6cf1c4b06d13634cb7c0f`，见 `package-final.json`。旧 `package-check.json` 与原 SHA 保持不变。
- `git diff --check`：退出0。

聚焦 GREEN 原始日志：`reports/2026-09-19-deepsort/final-fix-green.txt`。包检查原始日志：`reports/2026-09-19-deepsort/final-fix-package.txt`。

## 自审与关注点

F1 的导入/导出闭环已由同一规范化序列化函数约束；结果报告仍可大于5MiB，文档与界面不再承诺其可重导入。下载过程中若遇到不受控序列化异常，Demo 显示稳定 `EXPORT_FAILED`，会话不变。本次按简报未重跑核心完整 `verify` 或门户全量检查；核心与发行运行时代码未改。
