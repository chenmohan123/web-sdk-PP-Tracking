# Task 1 审查：核心契约与 DeepSORT 关联

### 规格合规

- ✅ **规格通过。** 公共类型与入口契约完整：新增 `FeatureSpace`、`TrackerAlgorithm` 的 `deepsort`、`Detection.embedding`、`TrackingFrame.featureSpaceId` 及三个专属选项，同时根入口只增加类型导出，没有公开 DeepSORT 运行时辅助函数（`src/types.ts:1`、`src/index.ts:4`）。
- ✅ 严格选项符合设计：ByteTrack 仍为默认算法；三组算法专属选项互斥；`featureSpace` 只接受 `id`、`dimension`；范围、显式 `undefined`、未知键及图库标量容量均在创建实例时拒绝（`src/tracker.ts:9`、`src/tracker.ts:35`）。
- ✅ 严格帧输入符合设计：DeepSORT 的空帧也必须携带匹配的空间 ID，每个检测均在分数过滤前完成向量类型、维度、有限性、非零范数和稀疏槽位校验；其他算法继续忽略外观字段（`src/tracker.ts:66`、`src/deepsort.ts:5`、`tests/deepsort.test.ts:48`、`tests/deepsort.test.ts:57`）。
- ✅ 数学实现符合设计：向量先除最大绝对分量再求范数；外观距离取图库最小余弦距离；运动距离使用四维创新向量与预测协方差左上 `4x4 + 4I` 的逆矩阵，并对不可表示结果报告 `NUMERICAL_FAILURE`（`src/deepsort.ts:5`、`src/deepsort.ts:27`、`src/kalman.ts:66`、`tests/kalman.test.ts:32`）。
- ✅ 关联顺序与硬门控符合设计：已确认轨迹按 `lastSeenMs` 降序分组级联，同组通过既有 Hungarian 分配实现最大可行匹配数优先，再按外观距离优化；类别、运动和外观分别作为有效边条件，没有用低分哨兵代替硬门控（`src/tracker.ts:140`、`src/tracker.ts:147`、`src/assignment.ts:4`）。
- ✅ IoU 后备路径范围正确：只接收未确认轨迹及进入本帧时仍为 `tracked` 的未匹配轨迹，`lost` 轨迹不能通过 IoU 绕过外观门限；后备匹配成功后按规格更新图库（`src/tracker.ts:158`、`src/tracker.ts:182`、`tests/deepsort.test.ts:96`、`tests/deepsort.test.ts:115`）。
- ✅ 图库与事务状态有界且原子：图库追加时复制向量并删除最旧超额样本；轨迹、Kalman 状态、观测历史和图库均在事务副本中更新，结果成功构建后才提交时钟、ID 和轨迹；reset/dispose 丢弃实例持有状态（`src/deepsort.ts:38`、`src/tracker.ts:90`、`src/tracker.ts:215`、`src/tracker.ts:218`）。
- ✅ CPU/main 与版本报告保持 `web-sdk-pp-tracking@0.2.0-alpha.0`，本次 diff 未修改依赖清单，也未加入框架或模型运行时依赖（`src/tracker.ts:212`；审查包文件清单）。
- ⚠️ **无法由本 Task 1 diff 单独验证：** ESM/CJS/类型声明对第三策略的真实消费仍由 Task 2 扩充；实现者报告也明确当前 `check:package` 消费样例仍只覆盖两种算法。控制器应在对应任务核验。
- ⚠️ **无法由本 Task 1 diff 单独验证：** Demo 导入原子性、三算法 UI、manifest、双语文档、门户记录及浏览器布局属于后续任务，不应在本任务扩大审查范围。
- ⚠️ **无法由代码证明：** “未读取上游跟踪/Kalman 源码”和“未执行远程 GitHub 写入”属于过程来源声明；本 diff 没有复制痕迹或远程操作证据，但仍需控制器依据执行记录确认。
- ⚠️ **流程证据待控制器确认：** `task-1-report.md` 记录了聚焦测试、typecheck、完整测试、build 和 package 检查，但没有记录项目 `AGENTS.md` 要求的 SDK checker 修改前/后证据。此次只读审查按要求未补跑。

### 优点

- 边界测试不是只断言“不抛错”：相反向量换序验证 ID，lost 轨迹验证外观硬门控，远位移验证运动门控，新鲜组验证级联优先级，图库容量测试验证最旧样本淘汰后的行为（`tests/deepsort.test.ts:89`、`tests/deepsort.test.ts:96`、`tests/deepsort.test.ts:104`、`tests/deepsort.test.ts:122`、`tests/deepsort.test.ts:133`）。
- 稳健归一化覆盖 `Number.MAX_VALUE` 与 `Number.MIN_VALUE`，并覆盖普通数组、`Float32Array` 和配置对象的深复制（`tests/deepsort.test.ts:74`、`tests/deepsort.test.ts:87`）。
- 数值失败测试在同一时间戳重试并验证原 ID/命中继续使用，直接覆盖预测和关联已发生后的事务回滚风险（`tests/deepsort.test.ts:166`、`src/tracker.ts:215`）。
- 聚焦旁查确认 `assign` 的虚拟列代价保证最大可行匹配数优先，且自定义有效边允许余弦相似度 `-1` 的合法边；旁查还确认状态只在结果完成后提交（`src/assignment.ts:7`、`src/assignment.ts:12`、`src/tracker.ts:207`、`src/tracker.ts:215`）。

### 问题

#### Critical（必须修复）

无。

#### Important（应修复）

无。

#### Minor（可改进）

无。

### 评估

**任务质量：通过。**

**理由：** 实现将外观、运动、类别三类约束保持为独立硬门控，级联和 IoU 后备边界与设计一致；输入归一化、图库容量、事务复制及失败回滚均有对应行为测试。现有未验证项属于后续任务或过程证据，不构成 Task 1 代码缺陷。
